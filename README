#execute the command and follow the result prompt to execute /tmp/install_all.sh
bash < <(curl -s https://raw.github.com/qianthinking/dotfiles/master/install_all_noi.sh)

#re-login, you will get new prompt of bash
