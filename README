#install default without customization
bash < <(curl -s https://raw.github.com/qianthinking/dotfiles/master/install_default.sh)

#install all in interactive shell with customization
bash < <(curl -s https://raw.github.com/qianthinking/dotfiles/master/install_all.sh)

#re-login, you will get new prompt of bash
